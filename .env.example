# Template file for local development.
# Copy to .env and fill in real values. .env is gitignored.

# Shared secret the ESP32 must send in the X-API-Key header to POST readings.
# Generate one with: openssl rand -hex 32
API_KEY=

# Where the SQLite file lives. Defaults to ./sensors.db if unset.
DB_PATH=sensors.db
